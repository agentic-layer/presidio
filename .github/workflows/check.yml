name: Check
on:
  push:
    branches:
      - main
      - 'renovate/**'
  pull_request:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start service
        run: docker compose up --build -d

      - name: Wait for service to be healthy
        run: docker compose wait presidio

      - name: E2E Test
        run: make e2e-test

      - name: Stop service
        if: always()
        run: docker compose down
